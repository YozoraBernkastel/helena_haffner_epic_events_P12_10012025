from control.support_controller import SupportController
from tests.mock import q

# def test_home_menu(support_controller, monkeypatch):
#     controller: SupportController = support_controller
#
#     inputs: list = ["1", "1", q]
#     monkeypatch.setattr('builtins.input', lambda _: inputs.pop(0))
#     controller.home_menu()
#

